return {
	{
		"{{nvim_theme}}",
        name = "theme"
		lazy = false,
		priority = 1000,
		config = function()
			require("theme").setup({
				transparent_background = true,
			})
			vim.cmd.colorscheme("{{nvim_colorscheme}}")
		end,
	},
}