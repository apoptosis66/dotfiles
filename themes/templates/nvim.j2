return {
	{
		"{{nvim_theme}}",
		name = "{{nvim_name}}",
		lazy = false,
		priority = 1000,
		config = function()
			require("{{nvim_name}}").setup({
				transparent_background = true,
			})
			vim.cmd.colorscheme("{{nvim_colorscheme}}")
		end,
	},
}